const Course = require('../models/Course');
const UserSession = require('../models/UserSession');

const CourseHelpers = {
    deleteHelper: async (token, courseId) => {
        //DB delete route code
    }
}

module.exports = CourseHelpers;